{% set ad_url = path('ads_show' , {slug: ad.slug})  %}
<div class="col-md-4">
    <div class="card bg-light mb-3">
        <div class="card-header text-center">
            {{ad.rooms}} chambres</ch> , <strong>{{ad.price}} &euro; / nuit</strong><br>
            <small>pas enore noté</small>
        </div>
        <a href="{{ ad_url }}">
            <img src="{{ ad.coverImage }}" alt="image de l'apprtement" style="height: 200px ;width: 100%;display: block;">
        </a>
        <div class="card-body">
            <h4 class="card-title">
                <a href="{{ ad_url }}">  {{ ad.title }}</a>
            </h4>
            <p class="card-text">
                {{ ad.introduction }}
            </p>
            <a href="{{ ad_url }}" class="btn btn-secondary">En savoir plus !</a>
        </div>
    </div>
</div>